HEVA no longer uses the legacy positional CLI that used to be recorded in this file.

Use the sectioned config interface instead:

  cd src
  ./assemble --config ../checks/fixtures/smoke_config.in --max-sweeps 5

The maintained command-line contract is documented in `docs/running-heva.md`
and the config format is documented in `docs/config-format.md`.
